/**
 * JAWARA-NET GLOBAL CONFIG TYPE
 * =============================
 *
 * Re-export of main configuration interface for backwards compatibility.
 */

// Re-export the main config from main.ts
export type { JawaraNetConfig, ChatResponse } from "./main";
